name: Cloud Run Deploy - immersive-lab

on:
  workflow_dispatch:
  push:
    branches:
      - master

env:
  PROJECT_ID: ${{ secrets.GCP_PROJECT_ID }}
  SERVICE_NAME: immersive-lab
  REGION: us-west1
  IMAGE_NAME: gcr.io/${{ secrets.GCP_PROJECT_ID }}/immersive-lab

jobs:
  deploy-cloud-run:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Google Auth
        uses: google-github-actions/auth@v1
        with:
          workload_identity_provider: '${{ secrets.GCP_WORKLOAD_IDENTITY_PROVIDER }}'
          service_account: '${{ secrets.GCP_SERVICE_ACCOUNT }}'

      - name: Setup gcloud CLI
        uses: google-github-actions/setup-gcloud@v1

      - name: Configure Docker
        run: gcloud auth configure-docker --quiet

      - name: Build Docker image
        run: |
          docker build -t $IMAGE_NAME:latest -t $IMAGE_NAME:${{ github.sha }} .

      - name: Push Docker image
        run: |
          docker push $IMAGE_NAME:latest
          docker push $IMAGE_NAME:${{ github.sha }}

      - name: Deploy to Cloud Run
        uses: google-github-actions/deploy-cloudrun@v1
        with:
          service: ${{ env.SERVICE_NAME }}
          image: $IMAGE_NAME:${{ github.sha }}
          region: ${{ env.REGION }}
          project_id: ${{ env.PROJECT_ID }}
          port: 3000 # Explicitly set the container port Cloud Run should target
          # Optional: Set any other Cloud Run deployment options here
          # env_vars: |
          #   KEY1=VALUE1
          #   KEY2=VALUE2
          # başına
          # memory: 256Mi
          # cpu: 1
          # timeout_seconds: 300
          # concurrency: 80
          # max_instances: 10
          # min_instances: 0
          # allow_unauthenticated: true # Set to false to require authentication
          # service_account_name: your-service-account@your-project.iam.gserviceaccount.com
          # vpc_connector: projects/your-project/locations/your-region/connectors/your-connector
          # vpc_egress: all-traffic
          # ingress: all
          # labels: |
          #   key1=value1
          #   key2=value2
          # revision_suffix: my-revision-suffix
          # traffic_percent: 100 # Percentage of traffic to route to the new revision
          # no_traffic: false # Set to true to deploy without routing traffic
          # tag: my-tag # Tag for the revision
          # flags: --add-cloudsql-instances=your-project:your-region:your-instance
          # client_name: gcloud-github-actions # Default client name
          # client_version: v1 # Default client version
          # skip_default_labels: false # Set to true to skip adding default labels
          # secrets: |
          #   SECRET_NAME1=projects/your-project/secrets/your-secret1:latest
          #   SECRET_NAME2=projects/your-project/secrets/your-secret2:1
          # cloudsql_instances: your-project:your-region:your-instance
          # command: '["/bin/sh", "-c", "your-command"]'
          # args: '["arg1", "arg2"]'
          # startup_cpu_boost: false # Set to true to enable startup CPU boost
          # execution_environment: gen1 # Default execution environment
          # container_and_port: '{"container":"your-container-name","port":8080}' # For sidecar deployments
          # network_interfaces: |
          #   - network: projects/your-project/global/networks/your-network
          #     subnetwork: projects/your-project/regions/your-region/subnetworks/your-subnetwork
          #     tags: '["tag1", "tag2"]'
          #     ip: static-ip-address # Optional static IP address
          #     network_attachment: projects/your-project/regions/your-region/networkAttachments/your-network-attachment
          # session_affinity: true # Enable session affinity
          # cpu_throttling: false # Disable CPU throttling
          # cpu_boost: false # Disable CPU boost
          # binary_authorization_policy: projects/your-project/binaryAuthorization/policies/your-policy # Enable Binary Authorization
          # binary_authorization_breakglass: false # Allow breakglass for Binary Authorization
          # custom_audiences: '["audience1", "audience2"]' # Custom audiences for Cloud Run Invoker
          # encryption_key: projects/your-project/locations/your-region/keyRings/your-key-ring/cryptoKeys/your-key # CMEK for Cloud Run
          # encryption_key_shutdown_hours: 0 # CMEK shutdown hours
          # encryption_key_revocation_action: PREVENT_NEW # CMEK revocation action
          # health_check_path: /healthz # Health check path
          # health_check_type: http # Health check type
          # health_check_interval_seconds: 5 # Health check interval
          # health_check_timeout_seconds: 5 # Health check timeout
          # health_check_healthy_threshold: 2 # Health check healthy threshold
          # health_check_unhealthy_threshold: 2 # Health check unhealthy threshold
          # health_check_grpc_service: your-grpc-service # gRPC health check service name
          # health_check_grpc_use_tls: false # Use TLS for gRPC health check
          # health_check_startup_path: /startup # Startup health check path
          # health_check_startup_interval_seconds: 5 # Startup health check interval
          # health_check_startup_timeout_seconds: 60 # Startup health check timeout
          # health_check_startup_failure_threshold: 1 # Startup health check failure threshold
          # health_check_startup_healthy_threshold: 1 # Startup health check healthy threshold
          # health_check_startup_grpc_service: your-grpc-service # Startup gRPC health check service name
          # health_check_startup_grpc_use_tls: false # Use TLS for startup gRPC health check
          # http2: true # Enable HTTP/2
          # idle_timeout_seconds: 300 # Idle timeout
          # region_tags: '{"key1":"value1","key2":"value2"}' # Region tags
          # service_labels: '{"key1":"value1","key2":"value2"}' # Service labels
          # service_annotations: '{"key1":"value1","key2":"value2"}' # Service annotations
          # revision_labels: '{"key1":"value1","key2":"value2"}' # Revision labels
          # revision_annotations: '{"key1":"value1","key2":"value2"}' # Revision annotations
          # source: . # Source directory for the build (if not using a pre-built image)
          # build_args: '["ARG1=value1", "ARG2=value2"]' # Build arguments (if building from source)
          # build_env_vars: '["KEY1=VALUE1", "KEY2=VALUE2"]' # Build environment variables (if building from source)
          # build_pack: gcr.io/buildpacks/builder:v1 # Buildpack builder (if building from source)
          # build_service_account: your-build-service-account@your-project.iam.gserviceaccount.com # Build service account (if building from source)
          # build_timeout: 600s # Build timeout (if building from source)
          # build_worker_pool: projects/your-project/locations/your-region/workerPools/your-worker-pool # Build worker pool (if building from source)
          # build_gcs_source_staging_dir: gs://your-bucket/staging # GCS source staging directory (if building from source)
          # build_gcs_log_dir: gs://your-bucket/logs # GCS log directory (if building from source)
          # build_dockerfile: Dockerfile # Dockerfile path (if building from source)
          # build_context_dir: . # Build context directory (if building from source)
          # build_network: projects/your-project/global/networks/your-network # Build network (if building from source)
          # build_disk_size_gb: 100 # Build disk size (if building from source)
          # build_machine_type: e2-medium # Build machine type (if building from source)
          # build_verbosity: info # Build verbosity (if building from source)
          # build_substitutions: '_KEY1=VALUE1,_KEY2=VALUE2' # Build substitutions (if building from source)
          # build_kaniko_cache_ttl: 6h # Kaniko cache TTL (if building from source with Kaniko)
          # build_kaniko_cache_repo: gcr.io/your-project/kaniko-cache # Kaniko cache repository (if building from source with Kaniko)
          # build_kaniko_cache_copy_layers: false # Copy layers to Kaniko cache (if building from source with Kaniko)
          # build_kaniko_cache: false # Enable Kaniko cache (if building from source with Kaniko)
          # build_kaniko_destination: $IMAGE_NAME:${{ github.sha }} # Kaniko destination (if building from source with Kaniko)
          # build_kaniko_image: gcr.io/kaniko-project/executor:latest # Kaniko image (if building from source with Kaniko)
          # build_kaniko_literal_args: '["--skip-tls-verify"]' # Kaniko literal arguments (if building from source with Kaniko)
          # build_kaniko_reproducible: false # Make Kaniko build reproducible (if building from source with Kaniko)
          # build_kaniko_snapshot_mode: full # Kaniko snapshot mode (if building from source with Kaniko)
          # build_kaniko_tar_path: /workspace/image.tar # Kaniko tar path (if building from source with Kaniko and creating a tarball)
          # build_kaniko_use_new_run: false # Use new run for Kaniko (if building from source with Kaniko)
          # build_kaniko_verbosity: info # Kaniko verbosity (if building from source with Kaniko)
          # build_kaniko_whitelist_var_run: false # Whitelist /var/run for Kaniko (if building from source with Kaniko)
          # build_kaniko_custom_flag: '["--custom-flag=value"]' # Kaniko custom flags (if building from source with Kaniko)
          # build_kaniko_build_args_file: build-args.txt # Kaniko build args file (if building from source with Kaniko)
          # build_kaniko_context_sub_path: . # Kaniko context sub path (if building from source with Kaniko)
          # build_kaniko_dockerfile_path: Dockerfile # Kaniko Dockerfile path (if building from source with Kaniko)
          # build_kaniko_image_fs_extract_retry: 0 # Kaniko image fs extract retry (if building from source with Kaniko)
          # build_kaniko_image_pull_retry: 0 # Kaniko image pull retry (if building from source with Kaniko)
          # build_kaniko_image_push_retry: 0 # Kaniko image push retry (if building from source with Kaniko)
          # build_kaniko_skip_push_permission_check: false # Skip push permission check for Kaniko (if building from source with Kaniko)
          # build_kaniko_skip_tls_verify_pull: false # Skip TLS verify pull for Kaniko (if building from source with Kaniko)
          # build_kaniko_skip_tls_verify_push: false # Skip TLS verify push for Kaniko (if building from source with Kaniko)
          # build_kaniko_skip_tls_verify_registry: false # Skip TLS verify registry for Kaniko (if building from source with Kaniko)
          # build_kaniko_snapshot_mode_redo: false # Redo snapshot mode for Kaniko (if building from source with Kaniko)
          # build_kaniko_ignore_paths: '["/ignore/this/path"]' # Ignore paths for Kaniko (if building from source with Kaniko)
          # build_kaniko_image_name_with_digest_file: image-name-with-digest.txt # Image name with digest file for Kaniko (if building from source with Kaniko)
          # build_kaniko_image_name_tag_with_digest_file: image-name-tag-with-digest.txt # Image name tag with digest file for Kaniko (if building from source with Kaniko)
          # build_kaniko_oci_layout_path: oci-layout # OCI layout path for Kaniko (if building from source with Kaniko)
          # build_kaniko_single_snapshot: false # Single snapshot for Kaniko (if building from source with Kaniko)
          # build_kaniko_tar_path_output: image.tar # Tar path output for Kaniko (if building from source with Kaniko)
          # build_kaniko_use_legacy_kaniko_snapshotter: false # Use legacy Kaniko snapshotter (if building from source with Kaniko)
          # build_kaniko_cleanup: false # Cleanup after Kaniko build (if building from source with Kaniko)
          # build_kaniko_compressed_caching: true # Compressed caching for Kaniko (if building from source with Kaniko)
          # build_kaniko_image_label: '{"key":"value"}' # Image label for Kaniko (if building from source with Kaniko)
          # build_kaniko_log_format: text # Log format for Kaniko (if building from source with Kaniko)
          # build_kaniko_log_timestamp: false # Log timestamp for Kaniko (if building from source with Kaniko)
          # build_kaniko_push_retry_count: 0 # Push retry count for Kaniko (if building from source with Kaniko)
          # build_kaniko_registry_mirror: mirror.gcr.io # Registry mirror for Kaniko (if building from source with Kaniko)
          # build_kaniko_registry_certificate: /path/to/cert # Registry certificate for Kaniko (if building from source with Kaniko)
          # build_kaniko_registry_insecure: false # Insecure registry for Kaniko (if building from source with Kaniko)
          # build_kaniko_registry_skip_tls_verify: false # Skip TLS verify registry for Kaniko (if building from source with Kaniko)
          # build_kaniko_skip_default_registry_fallback: false # Skip default registry fallback for Kaniko (if building from source with Kaniko)
          # build_kaniko_skip_unused_stages: false # Skip unused stages for Kaniko (if building from source with Kaniko)
          # build_kaniko_target: target-stage # Target stage for Kaniko (if building from source with Kaniko)
          # build_kaniko_image_fs_extract_retry_delay: 0s # Image fs extract retry delay for Kaniko (if building from source with Kaniko)
          # build_kaniko_image_pull_retry_delay: 0s # Image pull retry delay for Kaniko (if building from source with Kaniko)
          # build_kaniko_image_push_retry_delay: 0s # Image push retry delay for Kaniko (if building from source with Kaniko)
          # build_kaniko_push_retry_delay: 0s # Push retry delay for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_run_layers: false # Cache run layers for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_key: cache-key # Cache key for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_copy_layers_to_remote: false # Copy layers to remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_timeout: 0s # Remote cache push timeout for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_pull_timeout: 0s # Remote cache pull timeout for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_retry_count: 0 # Remote cache retry count for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_retry_delay: 0s # Remote cache retry delay for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_skip_tls_verify: false # Skip TLS verify for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_insecure: false # Insecure remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_mirror: mirror.gcr.io # Registry mirror for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_certificate: /path/to/cert # Registry certificate for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_skip_default_registry_fallback: false # Skip default registry fallback for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_permission_check: false # Skip push permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_pull_permission_check: false # Skip pull permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_with_digest_file: remote-image-name-with-digest.txt # Image name with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_tag_with_digest_file: remote-image-name-tag-with-digest.txt # Image name tag with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_oci_layout_path: remote-oci-layout # OCI layout path for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_single_snapshot: false # Single snapshot for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_tar_path_output: remote-image.tar # Tar path output for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_use_legacy_kaniko_snapshotter: false # Use legacy Kaniko snapshotter for remote cache (if building from source with Kaniko)
          # build_kaniko_cache_remote_cleanup: false # Cleanup after remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_compressed_caching: true # Compressed caching for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_label: '{"key":"value"}' # Image label for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_format: text # Log format for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_timestamp: false # Log timestamp for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_retry_count: 0 # Push retry count for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_mirror: mirror.gcr.io # Registry mirror for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_certificate: /path/to/cert # Registry certificate for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_insecure: false # Insecure remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_skip_tls_verify: false # Skip TLS verify for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_skip_default_registry_fallback: false # Skip default registry fallback for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_permission_check: false # Skip push permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_pull_permission_check: false # Skip pull permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_with_digest_file: remote-image-name-with-digest.txt # Image name with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_tag_with_digest_file: remote-image-name-tag-with-digest.txt # Image name tag with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_oci_layout_path: remote-oci-layout # OCI layout path for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_single_snapshot: false # Single snapshot for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_tar_path_output: remote-image.tar # Tar path output for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_use_legacy_kaniko_snapshotter: false # Use legacy Kaniko snapshotter for remote cache (if building from source with Kaniko)
          # build_kaniko_cache_remote_cleanup: false # Cleanup after remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_compressed_caching: true # Compressed caching for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_label: '{"key":"value"}' # Image label for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_format: text # Log format for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_timestamp: false # Log timestamp for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_retry_count: 0 # Push retry count for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_mirror: mirror.gcr.io # Registry mirror for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_certificate: /path/to/cert # Registry certificate for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_insecure: false # Insecure remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_skip_tls_verify: false # Skip TLS verify for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_skip_default_registry_fallback: false # Skip default registry fallback for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_permission_check: false # Skip push permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_pull_permission_check: false # Skip pull permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_with_digest_file: remote-image-name-with-digest.txt # Image name with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_tag_with_digest_file: remote-image-name-tag-with-digest.txt # Image name tag with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_oci_layout_path: remote-oci-layout # OCI layout path for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_single_snapshot: false # Single snapshot for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_tar_path_output: remote-image.tar # Tar path output for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_use_legacy_kaniko_snapshotter: false # Use legacy Kaniko snapshotter for remote cache (if building from source with Kaniko)
          # build_kaniko_cache_remote_cleanup: false # Cleanup after remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_compressed_caching: true # Compressed caching for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_label: '{"key":"value"}' # Image label for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_format: text # Log format for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_timestamp: false # Log timestamp for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_retry_count: 0 # Push retry count for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_mirror: mirror.gcr.io # Registry mirror for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_certificate: /path/to/cert # Registry certificate for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_insecure: false # Insecure remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_skip_tls_verify: false # Skip TLS verify for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_skip_default_registry_fallback: false # Skip default registry fallback for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_permission_check: false # Skip push permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_pull_permission_check: false # Skip pull permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_with_digest_file: remote-image-name-with-digest.txt # Image name with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_tag_with_digest_file: remote-image-name-tag-with-digest.txt # Image name tag with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_oci_layout_path: remote-oci-layout # OCI layout path for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_single_snapshot: false # Single snapshot for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_tar_path_output: remote-image.tar # Tar path output for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_use_legacy_kaniko_snapshotter: false # Use legacy Kaniko snapshotter for remote cache (if building from source with Kaniko)
          # build_kaniko_cache_remote_cleanup: false # Cleanup after remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_compressed_caching: true # Compressed caching for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_label: '{"key":"value"}' # Image label for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_format: text # Log format for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_timestamp: false # Log timestamp for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_retry_count: 0 # Push retry count for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_mirror: mirror.gcr.io # Registry mirror for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_certificate: /path/to/cert # Registry certificate for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_insecure: false # Insecure remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_skip_tls_verify: false # Skip TLS verify for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_skip_default_registry_fallback: false # Skip default registry fallback for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_permission_check: false # Skip push permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_pull_permission_check: false # Skip pull permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_with_digest_file: remote-image-name-with-digest.txt # Image name with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_tag_with_digest_file: remote-image-name-tag-with-digest.txt # Image name tag with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_oci_layout_path: remote-oci-layout # OCI layout path for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_single_snapshot: false # Single snapshot for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_tar_path_output: remote-image.tar # Tar path output for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_use_legacy_kaniko_snapshotter: false # Use legacy Kaniko snapshotter for remote cache (if building from source with Kaniko)
          # build_kaniko_cache_remote_cleanup: false # Cleanup after remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_compressed_caching: true # Compressed caching for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_label: '{"key":"value"}' # Image label for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_format: text # Log format for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_timestamp: false # Log timestamp for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_retry_count: 0 # Push retry count for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_mirror: mirror.gcr.io # Registry mirror for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_certificate: /path/to/cert # Registry certificate for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_insecure: false # Insecure remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_skip_tls_verify: false # Skip TLS verify for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_skip_default_registry_fallback: false # Skip default registry fallback for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_permission_check: false # Skip push permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_pull_permission_check: false # Skip pull permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_with_digest_file: remote-image-name-with-digest.txt # Image name with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_tag_with_digest_file: remote-image-name-tag-with-digest.txt # Image name tag with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_oci_layout_path: remote-oci-layout # OCI layout path for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_single_snapshot: false # Single snapshot for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_tar_path_output: remote-image.tar # Tar path output for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_use_legacy_kaniko_snapshotter: false # Use legacy Kaniko snapshotter for remote cache (if building from source with Kaniko)
          # build_kaniko_cache_remote_cleanup: false # Cleanup after remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_compressed_caching: true # Compressed caching for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_label: '{"key":"value"}' # Image label for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_format: text # Log format for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_timestamp: false # Log timestamp for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_retry_count: 0 # Push retry count for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_mirror: mirror.gcr.io # Registry mirror for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_certificate: /path/to/cert # Registry certificate for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_insecure: false # Insecure remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_skip_tls_verify: false # Skip TLS verify for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_skip_default_registry_fallback: false # Skip default registry fallback for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_permission_check: false # Skip push permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_pull_permission_check: false # Skip pull permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_with_digest_file: remote-image-name-with-digest.txt # Image name with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_tag_with_digest_file: remote-image-name-tag-with-digest.txt # Image name tag with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_oci_layout_path: remote-oci-layout # OCI layout path for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_single_snapshot: false # Single snapshot for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_tar_path_output: remote-image.tar # Tar path output for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_use_legacy_kaniko_snapshotter: false # Use legacy Kaniko snapshotter for remote cache (if building from source with Kaniko)
          # build_kaniko_cache_remote_cleanup: false # Cleanup after remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_compressed_caching: true # Compressed caching for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_label: '{"key":"value"}' # Image label for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_format: text # Log format for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_timestamp: false # Log timestamp for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_retry_count: 0 # Push retry count for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_mirror: mirror.gcr.io # Registry mirror for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_certificate: /path/to/cert # Registry certificate for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_insecure: false # Insecure remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_skip_tls_verify: false # Skip TLS verify for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_skip_default_registry_fallback: false # Skip default registry fallback for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_permission_check: false # Skip push permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_pull_permission_check: false # Skip pull permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_with_digest_file: remote-image-name-with-digest.txt # Image name with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_tag_with_digest_file: remote-image-name-tag-with-digest.txt # Image name tag with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_oci_layout_path: remote-oci-layout # OCI layout path for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_single_snapshot: false # Single snapshot for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_tar_path_output: remote-image.tar # Tar path output for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_use_legacy_kaniko_snapshotter: false # Use legacy Kaniko snapshotter for remote cache (if building from source with Kaniko)
          # build_kaniko_cache_remote_cleanup: false # Cleanup after remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_compressed_caching: true # Compressed caching for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_label: '{"key":"value"}' # Image label for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_format: text # Log format for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_timestamp: false # Log timestamp for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_retry_count: 0 # Push retry count for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_mirror: mirror.gcr.io # Registry mirror for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_certificate: /path/to/cert # Registry certificate for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_insecure: false # Insecure remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_skip_tls_verify: false # Skip TLS verify for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_skip_default_registry_fallback: false # Skip default registry fallback for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_permission_check: false # Skip push permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_pull_permission_check: false # Skip pull permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_with_digest_file: remote-image-name-with-digest.txt # Image name with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_tag_with_digest_file: remote-image-name-tag-with-digest.txt # Image name tag with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_oci_layout_path: remote-oci-layout # OCI layout path for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_single_snapshot: false # Single snapshot for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_tar_path_output: remote-image.tar # Tar path output for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_use_legacy_kaniko_snapshotter: false # Use legacy Kaniko snapshotter for remote cache (if building from source with Kaniko)
          # build_kaniko_cache_remote_cleanup: false # Cleanup after remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_compressed_caching: true # Compressed caching for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_label: '{"key":"value"}' # Image label for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_format: text # Log format for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_timestamp: false # Log timestamp for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_retry_count: 0 # Push retry count for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_mirror: mirror.gcr.io # Registry mirror for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_certificate: /path/to/cert # Registry certificate for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_insecure: false # Insecure remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_skip_tls_verify: false # Skip TLS verify for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_skip_default_registry_fallback: false # Skip default registry fallback for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_permission_check: false # Skip push permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_pull_permission_check: false # Skip pull permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_with_digest_file: remote-image-name-with-digest.txt # Image name with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_tag_with_digest_file: remote-image-name-tag-with-digest.txt # Image name tag with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_oci_layout_path: remote-oci-layout # OCI layout path for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_single_snapshot: false # Single snapshot for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_tar_path_output: remote-image.tar # Tar path output for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_use_legacy_kaniko_snapshotter: false # Use legacy Kaniko snapshotter for remote cache (if building from source with Kaniko)
          # build_kaniko_cache_remote_cleanup: false # Cleanup after remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_compressed_caching: true # Compressed caching for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_label: '{"key":"value"}' # Image label for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_format: text # Log format for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_timestamp: false # Log timestamp for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_retry_count: 0 # Push retry count for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_mirror: mirror.gcr.io # Registry mirror for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_certificate: /path/to/cert # Registry certificate for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_insecure: false # Insecure remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_skip_tls_verify: false # Skip TLS verify for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_skip_default_registry_fallback: false # Skip default registry fallback for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_permission_check: false # Skip push permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_pull_permission_check: false # Skip pull permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_with_digest_file: remote-image-name-with-digest.txt # Image name with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_tag_with_digest_file: remote-image-name-tag-with-digest.txt # Image name tag with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_oci_layout_path: remote-oci-layout # OCI layout path for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_single_snapshot: false # Single snapshot for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_tar_path_output: remote-image.tar # Tar path output for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_use_legacy_kaniko_snapshotter: false # Use legacy Kaniko snapshotter for remote cache (if building from source with Kaniko)
          # build_kaniko_cache_remote_cleanup: false # Cleanup after remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_compressed_caching: true # Compressed caching for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_label: '{"key":"value"}' # Image label for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_format: text # Log format for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_timestamp: false # Log timestamp for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_retry_count: 0 # Push retry count for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_mirror: mirror.gcr.io # Registry mirror for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_certificate: /path/to/cert # Registry certificate for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_insecure: false # Insecure remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_skip_tls_verify: false # Skip TLS verify for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_skip_default_registry_fallback: false # Skip default registry fallback for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_permission_check: false # Skip push permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_pull_permission_check: false # Skip pull permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_with_digest_file: remote-image-name-with-digest.txt # Image name with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_tag_with_digest_file: remote-image-name-tag-with-digest.txt # Image name tag with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_oci_layout_path: remote-oci-layout # OCI layout path for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_single_snapshot: false # Single snapshot for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_tar_path_output: remote-image.tar # Tar path output for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_use_legacy_kaniko_snapshotter: false # Use legacy Kaniko snapshotter for remote cache (if building from source with Kaniko)
          # build_kaniko_cache_remote_cleanup: false # Cleanup after remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_compressed_caching: true # Compressed caching for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_label: '{"key":"value"}' # Image label for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_format: text # Log format for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_timestamp: false # Log timestamp for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_retry_count: 0 # Push retry count for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_mirror: mirror.gcr.io # Registry mirror for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_certificate: /path/to/cert # Registry certificate for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_insecure: false # Insecure remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_skip_tls_verify: false # Skip TLS verify for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_skip_default_registry_fallback: false # Skip default registry fallback for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_permission_check: false # Skip push permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_pull_permission_check: false # Skip pull permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_with_digest_file: remote-image-name-with-digest.txt # Image name with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_tag_with_digest_file: remote-image-name-tag-with-digest.txt # Image name tag with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_oci_layout_path: remote-oci-layout # OCI layout path for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_single_snapshot: false # Single snapshot for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_tar_path_output: remote-image.tar # Tar path output for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_use_legacy_kaniko_snapshotter: false # Use legacy Kaniko snapshotter for remote cache (if building from source with Kaniko)
          # build_kaniko_cache_remote_cleanup: false # Cleanup after remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_compressed_caching: true # Compressed caching for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_label: '{"key":"value"}' # Image label for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_format: text # Log format for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_timestamp: false # Log timestamp for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_retry_count: 0 # Push retry count for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_mirror: mirror.gcr.io # Registry mirror for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_certificate: /path/to/cert # Registry certificate for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_insecure: false # Insecure remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_skip_tls_verify: false # Skip TLS verify for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_skip_default_registry_fallback: false # Skip default registry fallback for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_permission_check: false # Skip push permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_pull_permission_check: false # Skip pull permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_with_digest_file: remote-image-name-with-digest.txt # Image name with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_tag_with_digest_file: remote-image-name-tag-with-digest.txt # Image name tag with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_oci_layout_path: remote-oci-layout # OCI layout path for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_single_snapshot: false # Single snapshot for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_tar_path_output: remote-image.tar # Tar path output for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_use_legacy_kaniko_snapshotter: false # Use legacy Kaniko snapshotter for remote cache (if building from source with Kaniko)
          # build_kaniko_cache_remote_cleanup: false # Cleanup after remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_compressed_caching: true # Compressed caching for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_label: '{"key":"value"}' # Image label for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_format: text # Log format for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_timestamp: false # Log timestamp for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_retry_count: 0 # Push retry count for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_mirror: mirror.gcr.io # Registry mirror for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_certificate: /path/to/cert # Registry certificate for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_insecure: false # Insecure remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_skip_tls_verify: false # Skip TLS verify for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_skip_default_registry_fallback: false # Skip default registry fallback for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_permission_check: false # Skip push permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_pull_permission_check: false # Skip pull permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_with_digest_file: remote-image-name-with-digest.txt # Image name with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_tag_with_digest_file: remote-image-name-tag-with-digest.txt # Image name tag with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_oci_layout_path: remote-oci-layout # OCI layout path for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_single_snapshot: false # Single snapshot for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_tar_path_output: remote-image.tar # Tar path output for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_use_legacy_kaniko_snapshotter: false # Use legacy Kaniko snapshotter for remote cache (if building from source with Kaniko)
          # build_kaniko_cache_remote_cleanup: false # Cleanup after remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_compressed_caching: true # Compressed caching for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_label: '{"key":"value"}' # Image label for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_format: text # Log format for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_timestamp: false # Log timestamp for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_retry_count: 0 # Push retry count for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_mirror: mirror.gcr.io # Registry mirror for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_certificate: /path/to/cert # Registry certificate for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_insecure: false # Insecure remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_skip_tls_verify: false # Skip TLS verify for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_skip_default_registry_fallback: false # Skip default registry fallback for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_permission_check: false # Skip push permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_pull_permission_check: false # Skip pull permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_with_digest_file: remote-image-name-with-digest.txt # Image name with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_tag_with_digest_file: remote-image-name-tag-with-digest.txt # Image name tag with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_oci_layout_path: remote-oci-layout # OCI layout path for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_single_snapshot: false # Single snapshot for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_tar_path_output: remote-image.tar # Tar path output for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_use_legacy_kaniko_snapshotter: false # Use legacy Kaniko snapshotter for remote cache (if building from source with Kaniko)
          # build_kaniko_cache_remote_cleanup: false # Cleanup after remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_compressed_caching: true # Compressed caching for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_label: '{"key":"value"}' # Image label for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_format: text # Log format for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_timestamp: false # Log timestamp for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_retry_count: 0 # Push retry count for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_mirror: mirror.gcr.io # Registry mirror for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_certificate: /path/to/cert # Registry certificate for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_insecure: false # Insecure remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_skip_tls_verify: false # Skip TLS verify for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_skip_default_registry_fallback: false # Skip default registry fallback for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_permission_check: false # Skip push permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_pull_permission_check: false # Skip pull permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_with_digest_file: remote-image-name-with-digest.txt # Image name with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_tag_with_digest_file: remote-image-name-tag-with-digest.txt # Image name tag with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_oci_layout_path: remote-oci-layout # OCI layout path for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_single_snapshot: false # Single snapshot for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_tar_path_output: remote-image.tar # Tar path output for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_use_legacy_kaniko_snapshotter: false # Use legacy Kaniko snapshotter for remote cache (if building from source with Kaniko)
          # build_kaniko_cache_remote_cleanup: false # Cleanup after remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_compressed_caching: true # Compressed caching for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_label: '{"key":"value"}' # Image label for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_format: text # Log format for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_timestamp: false # Log timestamp for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_retry_count: 0 # Push retry count for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_mirror: mirror.gcr.io # Registry mirror for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_certificate: /path/to/cert # Registry certificate for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_insecure: false # Insecure remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_skip_tls_verify: false # Skip TLS verify for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_skip_default_registry_fallback: false # Skip default registry fallback for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_permission_check: false # Skip push permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_pull_permission_check: false # Skip pull permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_with_digest_file: remote-image-name-with-digest.txt # Image name with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_tag_with_digest_file: remote-image-name-tag-with-digest.txt # Image name tag with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_oci_layout_path: remote-oci-layout # OCI layout path for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_single_snapshot: false # Single snapshot for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_tar_path_output: remote-image.tar # Tar path output for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_use_legacy_kaniko_snapshotter: false # Use legacy Kaniko snapshotter for remote cache (if building from source with Kaniko)
          # build_kaniko_cache_remote_cleanup: false # Cleanup after remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_compressed_caching: true # Compressed caching for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_label: '{"key":"value"}' # Image label for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_format: text # Log format for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_timestamp: false # Log timestamp for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_retry_count: 0 # Push retry count for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_mirror: mirror.gcr.io # Registry mirror for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_certificate: /path/to/cert # Registry certificate for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_insecure: false # Insecure remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_skip_tls_verify: false # Skip TLS verify for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_skip_default_registry_fallback: false # Skip default registry fallback for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_permission_check: false # Skip push permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_pull_permission_check: false # Skip pull permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_with_digest_file: remote-image-name-with-digest.txt # Image name with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_tag_with_digest_file: remote-image-name-tag-with-digest.txt # Image name tag with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_oci_layout_path: remote-oci-layout # OCI layout path for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_single_snapshot: false # Single snapshot for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_tar_path_output: remote-image.tar # Tar path output for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_use_legacy_kaniko_snapshotter: false # Use legacy Kaniko snapshotter for remote cache (if building from source with Kaniko)
          # build_kaniko_cache_remote_cleanup: false # Cleanup after remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_compressed_caching: true # Compressed caching for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_label: '{"key":"value"}' # Image label for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_format: text # Log format for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_timestamp: false # Log timestamp for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_retry_count: 0 # Push retry count for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_mirror: mirror.gcr.io # Registry mirror for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_certificate: /path/to/cert # Registry certificate for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_insecure: false # Insecure remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_skip_tls_verify: false # Skip TLS verify for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_skip_default_registry_fallback: false # Skip default registry fallback for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_permission_check: false # Skip push permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_pull_permission_check: false # Skip pull permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_with_digest_file: remote-image-name-with-digest.txt # Image name with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_tag_with_digest_file: remote-image-name-tag-with-digest.txt # Image name tag with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_oci_layout_path: remote-oci-layout # OCI layout path for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_single_snapshot: false # Single snapshot for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_tar_path_output: remote-image.tar # Tar path output for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_use_legacy_kaniko_snapshotter: false # Use legacy Kaniko snapshotter for remote cache (if building from source with Kaniko)
          # build_kaniko_cache_remote_cleanup: false # Cleanup after remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_compressed_caching: true # Compressed caching for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_label: '{"key":"value"}' # Image label for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_format: text # Log format for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_timestamp: false # Log timestamp for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_retry_count: 0 # Push retry count for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_mirror: mirror.gcr.io # Registry mirror for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_certificate: /path/to/cert # Registry certificate for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_insecure: false # Insecure remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_skip_tls_verify: false # Skip TLS verify for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_skip_default_registry_fallback: false # Skip default registry fallback for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_permission_check: false # Skip push permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_pull_permission_check: false # Skip pull permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_with_digest_file: remote-image-name-with-digest.txt # Image name with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_tag_with_digest_file: remote-image-name-tag-with-digest.txt # Image name tag with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_oci_layout_path: remote-oci-layout # OCI layout path for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_single_snapshot: false # Single snapshot for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_tar_path_output: remote-image.tar # Tar path output for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_use_legacy_kaniko_snapshotter: false # Use legacy Kaniko snapshotter for remote cache (if building from source with Kaniko)
          # build_kaniko_cache_remote_cleanup: false # Cleanup after remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_compressed_caching: true # Compressed caching for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_label: '{"key":"value"}' # Image label for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_format: text # Log format for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_timestamp: false # Log timestamp for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_retry_count: 0 # Push retry count for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_mirror: mirror.gcr.io # Registry mirror for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_certificate: /path/to/cert # Registry certificate for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_insecure: false # Insecure remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_skip_tls_verify: false # Skip TLS verify for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_skip_default_registry_fallback: false # Skip default registry fallback for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_permission_check: false # Skip push permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_pull_permission_check: false # Skip pull permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_with_digest_file: remote-image-name-with-digest.txt # Image name with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_tag_with_digest_file: remote-image-name-tag-with-digest.txt # Image name tag with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_oci_layout_path: remote-oci-layout # OCI layout path for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_single_snapshot: false # Single snapshot for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_tar_path_output: remote-image.tar # Tar path output for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_use_legacy_kaniko_snapshotter: false # Use legacy Kaniko snapshotter for remote cache (if building from source with Kaniko)
          # build_kaniko_cache_remote_cleanup: false # Cleanup after remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_compressed_caching: true # Compressed caching for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_label: '{"key":"value"}' # Image label for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_format: text # Log format for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_timestamp: false # Log timestamp for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_retry_count: 0 # Push retry count for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_mirror: mirror.gcr.io # Registry mirror for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_certificate: /path/to/cert # Registry certificate for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_insecure: false # Insecure remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_skip_tls_verify: false # Skip TLS verify for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_skip_default_registry_fallback: false # Skip default registry fallback for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_permission_check: false # Skip push permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_pull_permission_check: false # Skip pull permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_with_digest_file: remote-image-name-with-digest.txt # Image name with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_tag_with_digest_file: remote-image-name-tag-with-digest.txt # Image name tag with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_oci_layout_path: remote-oci-layout # OCI layout path for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_single_snapshot: false # Single snapshot for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_tar_path_output: remote-image.tar # Tar path output for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_use_legacy_kaniko_snapshotter: false # Use legacy Kaniko snapshotter for remote cache (if building from source with Kaniko)
          # build_kaniko_cache_remote_cleanup: false # Cleanup after remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_compressed_caching: true # Compressed caching for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_label: '{"key":"value"}' # Image label for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_format: text # Log format for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_timestamp: false # Log timestamp for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_retry_count: 0 # Push retry count for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_mirror: mirror.gcr.io # Registry mirror for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_certificate: /path/to/cert # Registry certificate for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_insecure: false # Insecure remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_skip_tls_verify: false # Skip TLS verify for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_skip_default_registry_fallback: false # Skip default registry fallback for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_permission_check: false # Skip push permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_pull_permission_check: false # Skip pull permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_with_digest_file: remote-image-name-with-digest.txt # Image name with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_tag_with_digest_file: remote-image-name-tag-with-digest.txt # Image name tag with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_oci_layout_path: remote-oci-layout # OCI layout path for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_single_snapshot: false # Single snapshot for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_tar_path_output: remote-image.tar # Tar path output for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_use_legacy_kaniko_snapshotter: false # Use legacy Kaniko snapshotter for remote cache (if building from source with Kaniko)
          # build_kaniko_cache_remote_cleanup: false # Cleanup after remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_compressed_caching: true # Compressed caching for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_label: '{"key":"value"}' # Image label for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_format: text # Log format for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_timestamp: false # Log timestamp for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_retry_count: 0 # Push retry count for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_mirror: mirror.gcr.io # Registry mirror for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_certificate: /path/to/cert # Registry certificate for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_insecure: false # Insecure remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_skip_tls_verify: false # Skip TLS verify for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_skip_default_registry_fallback: false # Skip default registry fallback for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_permission_check: false # Skip push permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_pull_permission_check: false # Skip pull permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_with_digest_file: remote-image-name-with-digest.txt # Image name with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_tag_with_digest_file: remote-image-name-tag-with-digest.txt # Image name tag with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_oci_layout_path: remote-oci-layout # OCI layout path for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_single_snapshot: false # Single snapshot for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_tar_path_output: remote-image.tar # Tar path output for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_use_legacy_kaniko_snapshotter: false # Use legacy Kaniko snapshotter for remote cache (if building from source with Kaniko)
          # build_kaniko_cache_remote_cleanup: false # Cleanup after remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_compressed_caching: true # Compressed caching for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_label: '{"key":"value"}' # Image label for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_format: text # Log format for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_timestamp: false # Log timestamp for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_retry_count: 0 # Push retry count for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_mirror: mirror.gcr.io # Registry mirror for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_certificate: /path/to/cert # Registry certificate for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_insecure: false # Insecure remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_skip_tls_verify: false # Skip TLS verify for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_skip_default_registry_fallback: false # Skip default registry fallback for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_permission_check: false # Skip push permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_pull_permission_check: false # Skip pull permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_with_digest_file: remote-image-name-with-digest.txt # Image name with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_tag_with_digest_file: remote-image-name-tag-with-digest.txt # Image name tag with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_oci_layout_path: remote-oci-layout # OCI layout path for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_single_snapshot: false # Single snapshot for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_tar_path_output: remote-image.tar # Tar path output for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_use_legacy_kaniko_snapshotter: false # Use legacy Kaniko snapshotter for remote cache (if building from source with Kaniko)
          # build_kaniko_cache_remote_cleanup: false # Cleanup after remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_compressed_caching: true # Compressed caching for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_label: '{"key":"value"}' # Image label for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_format: text # Log format for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_timestamp: false # Log timestamp for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_retry_count: 0 # Push retry count for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_mirror: mirror.gcr.io # Registry mirror for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_certificate: /path/to/cert # Registry certificate for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_insecure: false # Insecure remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_skip_tls_verify: false # Skip TLS verify for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_skip_default_registry_fallback: false # Skip default registry fallback for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_permission_check: false # Skip push permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_pull_permission_check: false # Skip pull permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_with_digest_file: remote-image-name-with-digest.txt # Image name with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_tag_with_digest_file: remote-image-name-tag-with-digest.txt # Image name tag with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_oci_layout_path: remote-oci-layout # OCI layout path for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_single_snapshot: false # Single snapshot for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_tar_path_output: remote-image.tar # Tar path output for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_use_legacy_kaniko_snapshotter: false # Use legacy Kaniko snapshotter for remote cache (if building from source with Kaniko)
          # build_kaniko_cache_remote_cleanup: false # Cleanup after remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_compressed_caching: true # Compressed caching for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_label: '{"key":"value"}' # Image label for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_format: text # Log format for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_timestamp: false # Log timestamp for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_retry_count: 0 # Push retry count for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_mirror: mirror.gcr.io # Registry mirror for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_certificate: /path/to/cert # Registry certificate for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_insecure: false # Insecure remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_skip_tls_verify: false # Skip TLS verify for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_skip_default_registry_fallback: false # Skip default registry fallback for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_permission_check: false # Skip push permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_pull_permission_check: false # Skip pull permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_with_digest_file: remote-image-name-with-digest.txt # Image name with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_tag_with_digest_file: remote-image-name-tag-with-digest.txt # Image name tag with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_oci_layout_path: remote-oci-layout # OCI layout path for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_single_snapshot: false # Single snapshot for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_tar_path_output: remote-image.tar # Tar path output for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_use_legacy_kaniko_snapshotter: false # Use legacy Kaniko snapshotter for remote cache (if building from source with Kaniko)
          # build_kaniko_cache_remote_cleanup: false # Cleanup after remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_compressed_caching: true # Compressed caching for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_label: '{"key":"value"}' # Image label for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_format: text # Log format for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_timestamp: false # Log timestamp for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_retry_count: 0 # Push retry count for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_mirror: mirror.gcr.io # Registry mirror for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_certificate: /path/to/cert # Registry certificate for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_insecure: false # Insecure remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_skip_tls_verify: false # Skip TLS verify for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_skip_default_registry_fallback: false # Skip default registry fallback for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_permission_check: false # Skip push permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_pull_permission_check: false # Skip pull permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_with_digest_file: remote-image-name-with-digest.txt # Image name with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_tag_with_digest_file: remote-image-name-tag-with-digest.txt # Image name tag with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_oci_layout_path: remote-oci-layout # OCI layout path for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_single_snapshot: false # Single snapshot for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_tar_path_output: remote-image.tar # Tar path output for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_use_legacy_kaniko_snapshotter: false # Use legacy Kaniko snapshotter for remote cache (if building from source with Kaniko)
          # build_kaniko_cache_remote_cleanup: false # Cleanup after remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_compressed_caching: true # Compressed caching for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_label: '{"key":"value"}' # Image label for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_format: text # Log format for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_timestamp: false # Log timestamp for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_retry_count: 0 # Push retry count for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_mirror: mirror.gcr.io # Registry mirror for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_certificate: /path/to/cert # Registry certificate for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_insecure: false # Insecure remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_skip_tls_verify: false # Skip TLS verify for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_skip_default_registry_fallback: false # Skip default registry fallback for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_permission_check: false # Skip push permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_pull_permission_check: false # Skip pull permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_with_digest_file: remote-image-name-with-digest.txt # Image name with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_tag_with_digest_file: remote-image-name-tag-with-digest.txt # Image name tag with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_oci_layout_path: remote-oci-layout # OCI layout path for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_single_snapshot: false # Single snapshot for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_tar_path_output: remote-image.tar # Tar path output for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_use_legacy_kaniko_snapshotter: false # Use legacy Kaniko snapshotter for remote cache (if building from source with Kaniko)
          # build_kaniko_cache_remote_cleanup: false # Cleanup after remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_compressed_caching: true # Compressed caching for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_label: '{"key":"value"}' # Image label for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_format: text # Log format for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_timestamp: false # Log timestamp for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_retry_count: 0 # Push retry count for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_mirror: mirror.gcr.io # Registry mirror for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_certificate: /path/to/cert # Registry certificate for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_insecure: false # Insecure remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_skip_tls_verify: false # Skip TLS verify for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_skip_default_registry_fallback: false # Skip default registry fallback for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_permission_check: false # Skip push permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_pull_permission_check: false # Skip pull permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_with_digest_file: remote-image-name-with-digest.txt # Image name with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_tag_with_digest_file: remote-image-name-tag-with-digest.txt # Image name tag with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_oci_layout_path: remote-oci-layout # OCI layout path for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_single_snapshot: false # Single snapshot for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_tar_path_output: remote-image.tar # Tar path output for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_use_legacy_kaniko_snapshotter: false # Use legacy Kaniko snapshotter for remote cache (if building from source with Kaniko)
          # build_kaniko_cache_remote_cleanup: false # Cleanup after remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_compressed_caching: true # Compressed caching for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_label: '{"key":"value"}' # Image label for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_format: text # Log format for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_timestamp: false # Log timestamp for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_retry_count: 0 # Push retry count for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_mirror: mirror.gcr.io # Registry mirror for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_certificate: /path/to/cert # Registry certificate for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_insecure: false # Insecure remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_skip_tls_verify: false # Skip TLS verify for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_skip_default_registry_fallback: false # Skip default registry fallback for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_permission_check: false # Skip push permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_pull_permission_check: false # Skip pull permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_with_digest_file: remote-image-name-with-digest.txt # Image name with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_tag_with_digest_file: remote-image-name-tag-with-digest.txt # Image name tag with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_oci_layout_path: remote-oci-layout # OCI layout path for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_single_snapshot: false # Single snapshot for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_tar_path_output: remote-image.tar # Tar path output for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_use_legacy_kaniko_snapshotter: false # Use legacy Kaniko snapshotter for remote cache (if building from source with Kaniko)
          # build_kaniko_cache_remote_cleanup: false # Cleanup after remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_compressed_caching: true # Compressed caching for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_label: '{"key":"value"}' # Image label for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_format: text # Log format for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_timestamp: false # Log timestamp for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_retry_count: 0 # Push retry count for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_mirror: mirror.gcr.io # Registry mirror for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_certificate: /path/to/cert # Registry certificate for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_insecure: false # Insecure remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_skip_tls_verify: false # Skip TLS verify for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_skip_default_registry_fallback: false # Skip default registry fallback for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_permission_check: false # Skip push permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_pull_permission_check: false # Skip pull permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_with_digest_file: remote-image-name-with-digest.txt # Image name with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_tag_with_digest_file: remote-image-name-tag-with-digest.txt # Image name tag with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_oci_layout_path: remote-oci-layout # OCI layout path for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_single_snapshot: false # Single snapshot for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_tar_path_output: remote-image.tar # Tar path output for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_use_legacy_kaniko_snapshotter: false # Use legacy Kaniko snapshotter for remote cache (if building from source with Kaniko)
          # build_kaniko_cache_remote_cleanup: false # Cleanup after remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_compressed_caching: true # Compressed caching for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_label: '{"key":"value"}' # Image label for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_format: text # Log format for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_timestamp: false # Log timestamp for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_retry_count: 0 # Push retry count for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_mirror: mirror.gcr.io # Registry mirror for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_certificate: /path/to/cert # Registry certificate for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_insecure: false # Insecure remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_skip_tls_verify: false # Skip TLS verify for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_skip_default_registry_fallback: false # Skip default registry fallback for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_permission_check: false # Skip push permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_pull_permission_check: false # Skip pull permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_with_digest_file: remote-image-name-with-digest.txt # Image name with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_tag_with_digest_file: remote-image-name-tag-with-digest.txt # Image name tag with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_oci_layout_path: remote-oci-layout # OCI layout path for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_single_snapshot: false # Single snapshot for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_tar_path_output: remote-image.tar # Tar path output for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_use_legacy_kaniko_snapshotter: false # Use legacy Kaniko snapshotter for remote cache (if building from source with Kaniko)
          # build_kaniko_cache_remote_cleanup: false # Cleanup after remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_compressed_caching: true # Compressed caching for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_label: '{"key":"value"}' # Image label for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_format: text # Log format for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_timestamp: false # Log timestamp for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_retry_count: 0 # Push retry count for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_mirror: mirror.gcr.io # Registry mirror for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_certificate: /path/to/cert # Registry certificate for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_insecure: false # Insecure remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_skip_tls_verify: false # Skip TLS verify for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_skip_default_registry_fallback: false # Skip default registry fallback for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_permission_check: false # Skip push permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_pull_permission_check: false # Skip pull permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_with_digest_file: remote-image-name-with-digest.txt # Image name with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_tag_with_digest_file: remote-image-name-tag-with-digest.txt # Image name tag with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_oci_layout_path: remote-oci-layout # OCI layout path for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_single_snapshot: false # Single snapshot for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_tar_path_output: remote-image.tar # Tar path output for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_use_legacy_kaniko_snapshotter: false # Use legacy Kaniko snapshotter for remote cache (if building from source with Kaniko)
          # build_kaniko_cache_remote_cleanup: false # Cleanup after remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_compressed_caching: true # Compressed caching for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_label: '{"key":"value"}' # Image label for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_format: text # Log format for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_timestamp: false # Log timestamp for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_retry_count: 0 # Push retry count for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_mirror: mirror.gcr.io # Registry mirror for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_certificate: /path/to/cert # Registry certificate for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_insecure: false # Insecure remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_skip_tls_verify: false # Skip TLS verify for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_skip_default_registry_fallback: false # Skip default registry fallback for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_permission_check: false # Skip push permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_pull_permission_check: false # Skip pull permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_with_digest_file: remote-image-name-with-digest.txt # Image name with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_tag_with_digest_file: remote-image-name-tag-with-digest.txt # Image name tag with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_oci_layout_path: remote-oci-layout # OCI layout path for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_single_snapshot: false # Single snapshot for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_tar_path_output: remote-image.tar # Tar path output for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_use_legacy_kaniko_snapshotter: false # Use legacy Kaniko snapshotter for remote cache (if building from source with Kaniko)
          # build_kaniko_cache_remote_cleanup: false # Cleanup after remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_compressed_caching: true # Compressed caching for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_label: '{"key":"value"}' # Image label for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_format: text # Log format for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_timestamp: false # Log timestamp for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_retry_count: 0 # Push retry count for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_mirror: mirror.gcr.io # Registry mirror for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_certificate: /path/to/cert # Registry certificate for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_insecure: false # Insecure remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_skip_tls_verify: false # Skip TLS verify for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_skip_default_registry_fallback: false # Skip default registry fallback for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_permission_check: false # Skip push permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_pull_permission_check: false # Skip pull permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_with_digest_file: remote-image-name-with-digest.txt # Image name with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_tag_with_digest_file: remote-image-name-tag-with-digest.txt # Image name tag with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_oci_layout_path: remote-oci-layout # OCI layout path for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_single_snapshot: false # Single snapshot for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_tar_path_output: remote-image.tar # Tar path output for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_use_legacy_kaniko_snapshotter: false # Use legacy Kaniko snapshotter for remote cache (if building from source with Kaniko)
          # build_kaniko_cache_remote_cleanup: false # Cleanup after remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_compressed_caching: true # Compressed caching for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_label: '{"key":"value"}' # Image label for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_format: text # Log format for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_timestamp: false # Log timestamp for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_retry_count: 0 # Push retry count for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_mirror: mirror.gcr.io # Registry mirror for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_certificate: /path/to/cert # Registry certificate for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_insecure: false # Insecure remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_skip_tls_verify: false # Skip TLS verify for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_skip_default_registry_fallback: false # Skip default registry fallback for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_permission_check: false # Skip push permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_pull_permission_check: false # Skip pull permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_with_digest_file: remote-image-name-with-digest.txt # Image name with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_tag_with_digest_file: remote-image-name-tag-with-digest.txt # Image name tag with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_oci_layout_path: remote-oci-layout # OCI layout path for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_single_snapshot: false # Single snapshot for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_tar_path_output: remote-image.tar # Tar path output for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_use_legacy_kaniko_snapshotter: false # Use legacy Kaniko snapshotter for remote cache (if building from source with Kaniko)
          # build_kaniko_cache_remote_cleanup: false # Cleanup after remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_compressed_caching: true # Compressed caching for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_label: '{"key":"value"}' # Image label for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_format: text # Log format for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_timestamp: false # Log timestamp for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_retry_count: 0 # Push retry count for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_mirror: mirror.gcr.io # Registry mirror for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_certificate: /path/to/cert # Registry certificate for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_insecure: false # Insecure remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_skip_tls_verify: false # Skip TLS verify for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_skip_default_registry_fallback: false # Skip default registry fallback for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_permission_check: false # Skip push permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_pull_permission_check: false # Skip pull permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_with_digest_file: remote-image-name-with-digest.txt # Image name with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_tag_with_digest_file: remote-image-name-tag-with-digest.txt # Image name tag with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_oci_layout_path: remote-oci-layout # OCI layout path for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_single_snapshot: false # Single snapshot for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_tar_path_output: remote-image.tar # Tar path output for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_use_legacy_kaniko_snapshotter: false # Use legacy Kaniko snapshotter for remote cache (if building from source with Kaniko)
          # build_kaniko_cache_remote_cleanup: false # Cleanup after remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_compressed_caching: true # Compressed caching for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_label: '{"key":"value"}' # Image label for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_format: text # Log format for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_timestamp: false # Log timestamp for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_retry_count: 0 # Push retry count for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_mirror: mirror.gcr.io # Registry mirror for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_certificate: /path/to/cert # Registry certificate for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_insecure: false # Insecure remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_skip_tls_verify: false # Skip TLS verify for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_skip_default_registry_fallback: false # Skip default registry fallback for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_permission_check: false # Skip push permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_pull_permission_check: false # Skip pull permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_with_digest_file: remote-image-name-with-digest.txt # Image name with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_tag_with_digest_file: remote-image-name-tag-with-digest.txt # Image name tag with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_oci_layout_path: remote-oci-layout # OCI layout path for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_single_snapshot: false # Single snapshot for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_tar_path_output: remote-image.tar # Tar path output for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_use_legacy_kaniko_snapshotter: false # Use legacy Kaniko snapshotter for remote cache (if building from source with Kaniko)
          # build_kaniko_cache_remote_cleanup: false # Cleanup after remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_compressed_caching: true # Compressed caching for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_label: '{"key":"value"}' # Image label for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_format: text # Log format for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_timestamp: false # Log timestamp for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_retry_count: 0 # Push retry count for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_mirror: mirror.gcr.io # Registry mirror for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_certificate: /path/to/cert # Registry certificate for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_insecure: false # Insecure remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_skip_tls_verify: false # Skip TLS verify for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_skip_default_registry_fallback: false # Skip default registry fallback for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_permission_check: false # Skip push permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_pull_permission_check: false # Skip pull permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_with_digest_file: remote-image-name-with-digest.txt # Image name with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_tag_with_digest_file: remote-image-name-tag-with-digest.txt # Image name tag with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_oci_layout_path: remote-oci-layout # OCI layout path for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_single_snapshot: false # Single snapshot for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_tar_path_output: remote-image.tar # Tar path output for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_use_legacy_kaniko_snapshotter: false # Use legacy Kaniko snapshotter for remote cache (if building from source with Kaniko)
          # build_kaniko_cache_remote_cleanup: false # Cleanup after remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_compressed_caching: true # Compressed caching for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_label: '{"key":"value"}' # Image label for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_format: text # Log format for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_timestamp: false # Log timestamp for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_retry_count: 0 # Push retry count for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_mirror: mirror.gcr.io # Registry mirror for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_certificate: /path/to/cert # Registry certificate for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_insecure: false # Insecure remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_skip_tls_verify: false # Skip TLS verify for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_skip_default_registry_fallback: false # Skip default registry fallback for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_permission_check: false # Skip push permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_pull_permission_check: false # Skip pull permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_with_digest_file: remote-image-name-with-digest.txt # Image name with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_tag_with_digest_file: remote-image-name-tag-with-digest.txt # Image name tag with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_oci_layout_path: remote-oci-layout # OCI layout path for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_single_snapshot: false # Single snapshot for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_tar_path_output: remote-image.tar # Tar path output for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_use_legacy_kaniko_snapshotter: false # Use legacy Kaniko snapshotter for remote cache (if building from source with Kaniko)
          # build_kaniko_cache_remote_cleanup: false # Cleanup after remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_compressed_caching: true # Compressed caching for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_label: '{"key":"value"}' # Image label for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_format: text # Log format for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_timestamp: false # Log timestamp for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_retry_count: 0 # Push retry count for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_mirror: mirror.gcr.io # Registry mirror for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_certificate: /path/to/cert # Registry certificate for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_insecure: false # Insecure remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_skip_tls_verify: false # Skip TLS verify for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_skip_default_registry_fallback: false # Skip default registry fallback for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_permission_check: false # Skip push permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_pull_permission_check: false # Skip pull permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_with_digest_file: remote-image-name-with-digest.txt # Image name with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_tag_with_digest_file: remote-image-name-tag-with-digest.txt # Image name tag with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_oci_layout_path: remote-oci-layout # OCI layout path for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_single_snapshot: false # Single snapshot for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_tar_path_output: remote-image.tar # Tar path output for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_use_legacy_kaniko_snapshotter: false # Use legacy Kaniko snapshotter for remote cache (if building from source with Kaniko)
          # build_kaniko_cache_remote_cleanup: false # Cleanup after remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_compressed_caching: true # Compressed caching for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_label: '{"key":"value"}' # Image label for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_format: text # Log format for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_timestamp: false # Log timestamp for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_retry_count: 0 # Push retry count for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_mirror: mirror.gcr.io # Registry mirror for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_certificate: /path/to/cert # Registry certificate for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_insecure: false # Insecure remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_skip_tls_verify: false # Skip TLS verify for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_skip_default_registry_fallback: false # Skip default registry fallback for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_permission_check: false # Skip push permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_pull_permission_check: false # Skip pull permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_with_digest_file: remote-image-name-with-digest.txt # Image name with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_tag_with_digest_file: remote-image-name-tag-with-digest.txt # Image name tag with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_oci_layout_path: remote-oci-layout # OCI layout path for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_single_snapshot: false # Single snapshot for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_tar_path_output: remote-image.tar # Tar path output for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_use_legacy_kaniko_snapshotter: false # Use legacy Kaniko snapshotter for remote cache (if building from source with Kaniko)
          # build_kaniko_cache_remote_cleanup: false # Cleanup after remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_compressed_caching: true # Compressed caching for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_label: '{"key":"value"}' # Image label for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_format: text # Log format for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_timestamp: false # Log timestamp for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_retry_count: 0 # Push retry count for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_mirror: mirror.gcr.io # Registry mirror for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_certificate: /path/to/cert # Registry certificate for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_insecure: false # Insecure remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_skip_tls_verify: false # Skip TLS verify for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_skip_default_registry_fallback: false # Skip default registry fallback for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_permission_check: false # Skip push permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_pull_permission_check: false # Skip pull permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_with_digest_file: remote-image-name-with-digest.txt # Image name with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_tag_with_digest_file: remote-image-name-tag-with-digest.txt # Image name tag with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_oci_layout_path: remote-oci-layout # OCI layout path for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_single_snapshot: false # Single snapshot for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_tar_path_output: remote-image.tar # Tar path output for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_use_legacy_kaniko_snapshotter: false # Use legacy Kaniko snapshotter for remote cache (if building from source with Kaniko)
          # build_kaniko_cache_remote_cleanup: false # Cleanup after remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_compressed_caching: true # Compressed caching for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_label: '{"key":"value"}' # Image label for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_format: text # Log format for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_timestamp: false # Log timestamp for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_retry_count: 0 # Push retry count for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_mirror: mirror.gcr.io # Registry mirror for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_certificate: /path/to/cert # Registry certificate for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_insecure: false # Insecure remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_skip_tls_verify: false # Skip TLS verify for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_skip_default_registry_fallback: false # Skip default registry fallback for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_permission_check: false # Skip push permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_pull_permission_check: false # Skip pull permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_with_digest_file: remote-image-name-with-digest.txt # Image name with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_tag_with_digest_file: remote-image-name-tag-with-digest.txt # Image name tag with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_oci_layout_path: remote-oci-layout # OCI layout path for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_single_snapshot: false # Single snapshot for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_tar_path_output: remote-image.tar # Tar path output for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_use_legacy_kaniko_snapshotter: false # Use legacy Kaniko snapshotter for remote cache (if building from source with Kaniko)
          # build_kaniko_cache_remote_cleanup: false # Cleanup after remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_compressed_caching: true # Compressed caching for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_label: '{"key":"value"}' # Image label for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_format: text # Log format for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_timestamp: false # Log timestamp for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_retry_count: 0 # Push retry count for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_mirror: mirror.gcr.io # Registry mirror for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_certificate: /path/to/cert # Registry certificate for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_insecure: false # Insecure remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_skip_tls_verify: false # Skip TLS verify for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_skip_default_registry_fallback: false # Skip default registry fallback for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_permission_check: false # Skip push permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_pull_permission_check: false # Skip pull permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_with_digest_file: remote-image-name-with-digest.txt # Image name with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_tag_with_digest_file: remote-image-name-tag-with-digest.txt # Image name tag with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_oci_layout_path: remote-oci-layout # OCI layout path for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_single_snapshot: false # Single snapshot for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_tar_path_output: remote-image.tar # Tar path output for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_use_legacy_kaniko_snapshotter: false # Use legacy Kaniko snapshotter for remote cache (if building from source with Kaniko)
          # build_kaniko_cache_remote_cleanup: false # Cleanup after remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_compressed_caching: true # Compressed caching for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_label: '{"key":"value"}' # Image label for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_format: text # Log format for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_timestamp: false # Log timestamp for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_retry_count: 0 # Push retry count for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_mirror: mirror.gcr.io # Registry mirror for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_certificate: /path/to/cert # Registry certificate for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_insecure: false # Insecure remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_skip_tls_verify: false # Skip TLS verify for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_skip_default_registry_fallback: false # Skip default registry fallback for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_permission_check: false # Skip push permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_pull_permission_check: false # Skip pull permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_with_digest_file: remote-image-name-with-digest.txt # Image name with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_tag_with_digest_file: remote-image-name-tag-with-digest.txt # Image name tag with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_oci_layout_path: remote-oci-layout # OCI layout path for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_single_snapshot: false # Single snapshot for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_tar_path_output: remote-image.tar # Tar path output for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_use_legacy_kaniko_snapshotter: false # Use legacy Kaniko snapshotter for remote cache (if building from source with Kaniko)
          # build_kaniko_cache_remote_cleanup: false # Cleanup after remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_compressed_caching: true # Compressed caching for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_label: '{"key":"value"}' # Image label for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_format: text # Log format for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_timestamp: false # Log timestamp for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_retry_count: 0 # Push retry count for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_mirror: mirror.gcr.io # Registry mirror for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_certificate: /path/to/cert # Registry certificate for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_insecure: false # Insecure remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_skip_tls_verify: false # Skip TLS verify for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_skip_default_registry_fallback: false # Skip default registry fallback for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_permission_check: false # Skip push permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_pull_permission_check: false # Skip pull permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_with_digest_file: remote-image-name-with-digest.txt # Image name with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_tag_with_digest_file: remote-image-name-tag-with-digest.txt # Image name tag with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_oci_layout_path: remote-oci-layout # OCI layout path for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_single_snapshot: false # Single snapshot for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_tar_path_output: remote-image.tar # Tar path output for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_use_legacy_kaniko_snapshotter: false # Use legacy Kaniko snapshotter for remote cache (if building from source with Kaniko)
          # build_kaniko_cache_remote_cleanup: false # Cleanup after remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_compressed_caching: true # Compressed caching for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_label: '{"key":"value"}' # Image label for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_format: text # Log format for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_timestamp: false # Log timestamp for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_retry_count: 0 # Push retry count for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_mirror: mirror.gcr.io # Registry mirror for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_certificate: /path/to/cert # Registry certificate for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_insecure: false # Insecure remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_skip_tls_verify: false # Skip TLS verify for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_skip_default_registry_fallback: false # Skip default registry fallback for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_permission_check: false # Skip push permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_pull_permission_check: false # Skip pull permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_with_digest_file: remote-image-name-with-digest.txt # Image name with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_tag_with_digest_file: remote-image-name-tag-with-digest.txt # Image name tag with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_oci_layout_path: remote-oci-layout # OCI layout path for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_single_snapshot: false # Single snapshot for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_tar_path_output: remote-image.tar # Tar path output for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_use_legacy_kaniko_snapshotter: false # Use legacy Kaniko snapshotter for remote cache (if building from source with Kaniko)
          # build_kaniko_cache_remote_cleanup: false # Cleanup after remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_compressed_caching: true # Compressed caching for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_label: '{"key":"value"}' # Image label for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_format: text # Log format for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_timestamp: false # Log timestamp for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_retry_count: 0 # Push retry count for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_mirror: mirror.gcr.io # Registry mirror for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_certificate: /path/to/cert # Registry certificate for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_insecure: false # Insecure remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_skip_tls_verify: false # Skip TLS verify for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_skip_default_registry_fallback: false # Skip default registry fallback for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_permission_check: false # Skip push permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_pull_permission_check: false # Skip pull permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_with_digest_file: remote-image-name-with-digest.txt # Image name with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_tag_with_digest_file: remote-image-name-tag-with-digest.txt # Image name tag with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_oci_layout_path: remote-oci-layout # OCI layout path for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_single_snapshot: false # Single snapshot for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_tar_path_output: remote-image.tar # Tar path output for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_use_legacy_kaniko_snapshotter: false # Use legacy Kaniko snapshotter for remote cache (if building from source with Kaniko)
          # build_kaniko_cache_remote_cleanup: false # Cleanup after remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_compressed_caching: true # Compressed caching for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_label: '{"key":"value"}' # Image label for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_format: text # Log format for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_timestamp: false # Log timestamp for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_retry_count: 0 # Push retry count for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_mirror: mirror.gcr.io # Registry mirror for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_certificate: /path/to/cert # Registry certificate for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_insecure: false # Insecure remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_skip_tls_verify: false # Skip TLS verify for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_skip_default_registry_fallback: false # Skip default registry fallback for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_permission_check: false # Skip push permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_pull_permission_check: false # Skip pull permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_with_digest_file: remote-image-name-with-digest.txt # Image name with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_tag_with_digest_file: remote-image-name-tag-with-digest.txt # Image name tag with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_oci_layout_path: remote-oci-layout # OCI layout path for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_single_snapshot: false # Single snapshot for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_tar_path_output: remote-image.tar # Tar path output for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_use_legacy_kaniko_snapshotter: false # Use legacy Kaniko snapshotter for remote cache (if building from source with Kaniko)
          # build_kaniko_cache_remote_cleanup: false # Cleanup after remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_compressed_caching: true # Compressed caching for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_label: '{"key":"value"}' # Image label for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_format: text # Log format for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_timestamp: false # Log timestamp for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_retry_count: 0 # Push retry count for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_mirror: mirror.gcr.io # Registry mirror for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_certificate: /path/to/cert # Registry certificate for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_insecure: false # Insecure remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_skip_tls_verify: false # Skip TLS verify for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_skip_default_registry_fallback: false # Skip default registry fallback for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_permission_check: false # Skip push permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_pull_permission_check: false # Skip pull permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_with_digest_file: remote-image-name-with-digest.txt # Image name with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_tag_with_digest_file: remote-image-name-tag-with-digest.txt # Image name tag with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_oci_layout_path: remote-oci-layout # OCI layout path for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_single_snapshot: false # Single snapshot for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_tar_path_output: remote-image.tar # Tar path output for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_use_legacy_kaniko_snapshotter: false # Use legacy Kaniko snapshotter for remote cache (if building from source with Kaniko)
          # build_kaniko_cache_remote_cleanup: false # Cleanup after remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_compressed_caching: true # Compressed caching for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_label: '{"key":"value"}' # Image label for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_format: text # Log format for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_timestamp: false # Log timestamp for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_retry_count: 0 # Push retry count for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_mirror: mirror.gcr.io # Registry mirror for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_certificate: /path/to/cert # Registry certificate for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_insecure: false # Insecure remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_skip_tls_verify: false # Skip TLS verify for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_skip_default_registry_fallback: false # Skip default registry fallback for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_permission_check: false # Skip push permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_pull_permission_check: false # Skip pull permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_with_digest_file: remote-image-name-with-digest.txt # Image name with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_tag_with_digest_file: remote-image-name-tag-with-digest.txt # Image name tag with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_oci_layout_path: remote-oci-layout # OCI layout path for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_single_snapshot: false # Single snapshot for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_tar_path_output: remote-image.tar # Tar path output for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_use_legacy_kaniko_snapshotter: false # Use legacy Kaniko snapshotter for remote cache (if building from source with Kaniko)
          # build_kaniko_cache_remote_cleanup: false # Cleanup after remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_compressed_caching: true # Compressed caching for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_label: '{"key":"value"}' # Image label for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_format: text # Log format for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_timestamp: false # Log timestamp for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_retry_count: 0 # Push retry count for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_mirror: mirror.gcr.io # Registry mirror for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_certificate: /path/to/cert # Registry certificate for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_insecure: false # Insecure remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_skip_tls_verify: false # Skip TLS verify for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_skip_default_registry_fallback: false # Skip default registry fallback for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_permission_check: false # Skip push permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_pull_permission_check: false # Skip pull permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_with_digest_file: remote-image-name-with-digest.txt # Image name with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_tag_with_digest_file: remote-image-name-tag-with-digest.txt # Image name tag with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_oci_layout_path: remote-oci-layout # OCI layout path for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_single_snapshot: false # Single snapshot for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_tar_path_output: remote-image.tar # Tar path output for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_use_legacy_kaniko_snapshotter: false # Use legacy Kaniko snapshotter for remote cache (if building from source with Kaniko)
          # build_kaniko_cache_remote_cleanup: false # Cleanup after remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_compressed_caching: true # Compressed caching for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_label: '{"key":"value"}' # Image label for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_format: text # Log format for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_timestamp: false # Log timestamp for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_retry_count: 0 # Push retry count for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_mirror: mirror.gcr.io # Registry mirror for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_certificate: /path/to/cert # Registry certificate for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_insecure: false # Insecure remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_skip_tls_verify: false # Skip TLS verify for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_skip_default_registry_fallback: false # Skip default registry fallback for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_permission_check: false # Skip push permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_pull_permission_check: false # Skip pull permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_with_digest_file: remote-image-name-with-digest.txt # Image name with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_tag_with_digest_file: remote-image-name-tag-with-digest.txt # Image name tag with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_oci_layout_path: remote-oci-layout # OCI layout path for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_single_snapshot: false # Single snapshot for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_tar_path_output: remote-image.tar # Tar path output for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_use_legacy_kaniko_snapshotter: false # Use legacy Kaniko snapshotter for remote cache (if building from source with Kaniko)
          # build_kaniko_cache_remote_cleanup: false # Cleanup after remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_compressed_caching: true # Compressed caching for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_label: '{"key":"value"}' # Image label for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_format: text # Log format for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_timestamp: false # Log timestamp for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_retry_count: 0 # Push retry count for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_mirror: mirror.gcr.io # Registry mirror for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_certificate: /path/to/cert # Registry certificate for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_insecure: false # Insecure remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_skip_tls_verify: false # Skip TLS verify for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_skip_default_registry_fallback: false # Skip default registry fallback for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_permission_check: false # Skip push permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_pull_permission_check: false # Skip pull permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_with_digest_file: remote-image-name-with-digest.txt # Image name with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_tag_with_digest_file: remote-image-name-tag-with-digest.txt # Image name tag with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_oci_layout_path: remote-oci-layout # OCI layout path for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_single_snapshot: false # Single snapshot for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_tar_path_output: remote-image.tar # Tar path output for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_use_legacy_kaniko_snapshotter: false # Use legacy Kaniko snapshotter for remote cache (if building from source with Kaniko)
          # build_kaniko_cache_remote_cleanup: false # Cleanup after remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_compressed_caching: true # Compressed caching for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_label: '{"key":"value"}' # Image label for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_format: text # Log format for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_timestamp: false # Log timestamp for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_retry_count: 0 # Push retry count for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_mirror: mirror.gcr.io # Registry mirror for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_certificate: /path/to/cert # Registry certificate for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_insecure: false # Insecure remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_skip_tls_verify: false # Skip TLS verify for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_skip_default_registry_fallback: false # Skip default registry fallback for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_permission_check: false # Skip push permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_pull_permission_check: false # Skip pull permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_with_digest_file: remote-image-name-with-digest.txt # Image name with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_tag_with_digest_file: remote-image-name-tag-with-digest.txt # Image name tag with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_oci_layout_path: remote-oci-layout # OCI layout path for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_single_snapshot: false # Single snapshot for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_tar_path_output: remote-image.tar # Tar path output for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_use_legacy_kaniko_snapshotter: false # Use legacy Kaniko snapshotter for remote cache (if building from source with Kaniko)
          # build_kaniko_cache_remote_cleanup: false # Cleanup after remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_compressed_caching: true # Compressed caching for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_label: '{"key":"value"}' # Image label for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_format: text # Log format for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_timestamp: false # Log timestamp for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_retry_count: 0 # Push retry count for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_mirror: mirror.gcr.io # Registry mirror for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_certificate: /path/to/cert # Registry certificate for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_insecure: false # Insecure remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_skip_tls_verify: false # Skip TLS verify for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_skip_default_registry_fallback: false # Skip default registry fallback for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_permission_check: false # Skip push permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_pull_permission_check: false # Skip pull permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_with_digest_file: remote-image-name-with-digest.txt # Image name with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_tag_with_digest_file: remote-image-name-tag-with-digest.txt # Image name tag with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_oci_layout_path: remote-oci-layout # OCI layout path for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_single_snapshot: false # Single snapshot for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_tar_path_output: remote-image.tar # Tar path output for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_use_legacy_kaniko_snapshotter: false # Use legacy Kaniko snapshotter for remote cache (if building from source with Kaniko)
          # build_kaniko_cache_remote_cleanup: false # Cleanup after remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_compressed_caching: true # Compressed caching for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_label: '{"key":"value"}' # Image label for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_format: text # Log format for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_timestamp: false # Log timestamp for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_retry_count: 0 # Push retry count for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_mirror: mirror.gcr.io # Registry mirror for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_certificate: /path/to/cert # Registry certificate for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_insecure: false # Insecure remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_skip_tls_verify: false # Skip TLS verify for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_skip_default_registry_fallback: false # Skip default registry fallback for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_permission_check: false # Skip push permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_pull_permission_check: false # Skip pull permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_with_digest_file: remote-image-name-with-digest.txt # Image name with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_tag_with_digest_file: remote-image-name-tag-with-digest.txt # Image name tag with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_oci_layout_path: remote-oci-layout # OCI layout path for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_single_snapshot: false # Single snapshot for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_tar_path_output: remote-image.tar # Tar path output for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_use_legacy_kaniko_snapshotter: false # Use legacy Kaniko snapshotter for remote cache (if building from source with Kaniko)
          # build_kaniko_cache_remote_cleanup: false # Cleanup after remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_compressed_caching: true # Compressed caching for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_label: '{"key":"value"}' # Image label for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_format: text # Log format for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_timestamp: false # Log timestamp for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_retry_count: 0 # Push retry count for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_mirror: mirror.gcr.io # Registry mirror for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_certificate: /path/to/cert # Registry certificate for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_insecure: false # Insecure remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_skip_tls_verify: false # Skip TLS verify for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_skip_default_registry_fallback: false # Skip default registry fallback for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_permission_check: false # Skip push permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_pull_permission_check: false # Skip pull permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_with_digest_file: remote-image-name-with-digest.txt # Image name with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_tag_with_digest_file: remote-image-name-tag-with-digest.txt # Image name tag with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_oci_layout_path: remote-oci-layout # OCI layout path for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_single_snapshot: false # Single snapshot for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_tar_path_output: remote-image.tar # Tar path output for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_use_legacy_kaniko_snapshotter: false # Use legacy Kaniko snapshotter for remote cache (if building from source with Kaniko)
          # build_kaniko_cache_remote_cleanup: false # Cleanup after remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_compressed_caching: true # Compressed caching for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_label: '{"key":"value"}' # Image label for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_format: text # Log format for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_timestamp: false # Log timestamp for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_retry_count: 0 # Push retry count for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_mirror: mirror.gcr.io # Registry mirror for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_certificate: /path/to/cert # Registry certificate for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_insecure: false # Insecure remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_skip_tls_verify: false # Skip TLS verify for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_skip_default_registry_fallback: false # Skip default registry fallback for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_permission_check: false # Skip push permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_pull_permission_check: false # Skip pull permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_with_digest_file: remote-image-name-with-digest.txt # Image name with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_tag_with_digest_file: remote-image-name-tag-with-digest.txt # Image name tag with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_oci_layout_path: remote-oci-layout # OCI layout path for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_single_snapshot: false # Single snapshot for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_tar_path_output: remote-image.tar # Tar path output for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_use_legacy_kaniko_snapshotter: false # Use legacy Kaniko snapshotter for remote cache (if building from source with Kaniko)
          # build_kaniko_cache_remote_cleanup: false # Cleanup after remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_compressed_caching: true # Compressed caching for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_label: '{"key":"value"}' # Image label for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_format: text # Log format for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_timestamp: false # Log timestamp for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_retry_count: 0 # Push retry count for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_mirror: mirror.gcr.io # Registry mirror for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_certificate: /path/to/cert # Registry certificate for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_insecure: false # Insecure remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_skip_tls_verify: false # Skip TLS verify for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_skip_default_registry_fallback: false # Skip default registry fallback for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_permission_check: false # Skip push permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_pull_permission_check: false # Skip pull permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_with_digest_file: remote-image-name-with-digest.txt # Image name with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_tag_with_digest_file: remote-image-name-tag-with-digest.txt # Image name tag with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_oci_layout_path: remote-oci-layout # OCI layout path for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_single_snapshot: false # Single snapshot for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_tar_path_output: remote-image.tar # Tar path output for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_use_legacy_kaniko_snapshotter: false # Use legacy Kaniko snapshotter for remote cache (if building from source with Kaniko)
          # build_kaniko_cache_remote_cleanup: false # Cleanup after remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_compressed_caching: true # Compressed caching for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_label: '{"key":"value"}' # Image label for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_format: text # Log format for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_timestamp: false # Log timestamp for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_retry_count: 0 # Push retry count for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_mirror: mirror.gcr.io # Registry mirror for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_certificate: /path/to/cert # Registry certificate for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_insecure: false # Insecure remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_skip_tls_verify: false # Skip TLS verify for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_skip_default_registry_fallback: false # Skip default registry fallback for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_permission_check: false # Skip push permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_pull_permission_check: false # Skip pull permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_with_digest_file: remote-image-name-with-digest.txt # Image name with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_tag_with_digest_file: remote-image-name-tag-with-digest.txt # Image name tag with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_oci_layout_path: remote-oci-layout # OCI layout path for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_single_snapshot: false # Single snapshot for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_tar_path_output: remote-image.tar # Tar path output for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_use_legacy_kaniko_snapshotter: false # Use legacy Kaniko snapshotter for remote cache (if building from source with Kaniko)
          # build_kaniko_cache_remote_cleanup: false # Cleanup after remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_compressed_caching: true # Compressed caching for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_label: '{"key":"value"}' # Image label for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_format: text # Log format for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_timestamp: false # Log timestamp for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_retry_count: 0 # Push retry count for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_mirror: mirror.gcr.io # Registry mirror for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_certificate: /path/to/cert # Registry certificate for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_insecure: false # Insecure remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_skip_tls_verify: false # Skip TLS verify for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_skip_default_registry_fallback: false # Skip default registry fallback for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_permission_check: false # Skip push permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_pull_permission_check: false # Skip pull permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_with_digest_file: remote-image-name-with-digest.txt # Image name with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_tag_with_digest_file: remote-image-name-tag-with-digest.txt # Image name tag with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_oci_layout_path: remote-oci-layout # OCI layout path for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_single_snapshot: false # Single snapshot for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_tar_path_output: remote-image.tar # Tar path output for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_use_legacy_kaniko_snapshotter: false # Use legacy Kaniko snapshotter for remote cache (if building from source with Kaniko)
          # build_kaniko_cache_remote_cleanup: false # Cleanup after remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_compressed_caching: true # Compressed caching for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_label: '{"key":"value"}' # Image label for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_format: text # Log format for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_timestamp: false # Log timestamp for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_retry_count: 0 # Push retry count for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_mirror: mirror.gcr.io # Registry mirror for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_certificate: /path/to/cert # Registry certificate for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_insecure: false # Insecure remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_skip_tls_verify: false # Skip TLS verify for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_skip_default_registry_fallback: false # Skip default registry fallback for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_permission_check: false # Skip push permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_pull_permission_check: false # Skip pull permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_with_digest_file: remote-image-name-with-digest.txt # Image name with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_tag_with_digest_file: remote-image-name-tag-with-digest.txt # Image name tag with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_oci_layout_path: remote-oci-layout # OCI layout path for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_single_snapshot: false # Single snapshot for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_tar_path_output: remote-image.tar # Tar path output for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_use_legacy_kaniko_snapshotter: false # Use legacy Kaniko snapshotter for remote cache (if building from source with Kaniko)
          # build_kaniko_cache_remote_cleanup: false # Cleanup after remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_compressed_caching: true # Compressed caching for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_label: '{"key":"value"}' # Image label for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_format: text # Log format for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_timestamp: false # Log timestamp for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_retry_count: 0 # Push retry count for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_mirror: mirror.gcr.io # Registry mirror for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_certificate: /path/to/cert # Registry certificate for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_insecure: false # Insecure remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_skip_tls_verify: false # Skip TLS verify for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_skip_default_registry_fallback: false # Skip default registry fallback for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_permission_check: false # Skip push permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_pull_permission_check: false # Skip pull permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_with_digest_file: remote-image-name-with-digest.txt # Image name with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_tag_with_digest_file: remote-image-name-tag-with-digest.txt # Image name tag with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_oci_layout_path: remote-oci-layout # OCI layout path for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_single_snapshot: false # Single snapshot for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_tar_path_output: remote-image.tar # Tar path output for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_use_legacy_kaniko_snapshotter: false # Use legacy Kaniko snapshotter for remote cache (if building from source with Kaniko)
          # build_kaniko_cache_remote_cleanup: false # Cleanup after remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_compressed_caching: true # Compressed caching for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_label: '{"key":"value"}' # Image label for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_format: text # Log format for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_timestamp: false # Log timestamp for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_retry_count: 0 # Push retry count for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_mirror: mirror.gcr.io # Registry mirror for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_certificate: /path/to/cert # Registry certificate for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_insecure: false # Insecure remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_skip_tls_verify: false # Skip TLS verify for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_skip_default_registry_fallback: false # Skip default registry fallback for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_permission_check: false # Skip push permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_pull_permission_check: false # Skip pull permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_with_digest_file: remote-image-name-with-digest.txt # Image name with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_tag_with_digest_file: remote-image-name-tag-with-digest.txt # Image name tag with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_oci_layout_path: remote-oci-layout # OCI layout path for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_single_snapshot: false # Single snapshot for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_tar_path_output: remote-image.tar # Tar path output for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_use_legacy_kaniko_snapshotter: false # Use legacy Kaniko snapshotter for remote cache (if building from source with Kaniko)
          # build_kaniko_cache_remote_cleanup: false # Cleanup after remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_compressed_caching: true # Compressed caching for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_label: '{"key":"value"}' # Image label for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_format: text # Log format for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_timestamp: false # Log timestamp for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_retry_count: 0 # Push retry count for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_mirror: mirror.gcr.io # Registry mirror for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_certificate: /path/to/cert # Registry certificate for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_insecure: false # Insecure remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_skip_tls_verify: false # Skip TLS verify for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_skip_default_registry_fallback: false # Skip default registry fallback for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_permission_check: false # Skip push permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_pull_permission_check: false # Skip pull permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_with_digest_file: remote-image-name-with-digest.txt # Image name with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_tag_with_digest_file: remote-image-name-tag-with-digest.txt # Image name tag with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_oci_layout_path: remote-oci-layout # OCI layout path for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_single_snapshot: false # Single snapshot for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_tar_path_output: remote-image.tar # Tar path output for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_use_legacy_kaniko_snapshotter: false # Use legacy Kaniko snapshotter for remote cache (if building from source with Kaniko)
          # build_kaniko_cache_remote_cleanup: false # Cleanup after remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_compressed_caching: true # Compressed caching for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_label: '{"key":"value"}' # Image label for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_format: text # Log format for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_timestamp: false # Log timestamp for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_retry_count: 0 # Push retry count for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_mirror: mirror.gcr.io # Registry mirror for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_certificate: /path/to/cert # Registry certificate for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_insecure: false # Insecure remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_skip_tls_verify: false # Skip TLS verify for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_skip_default_registry_fallback: false # Skip default registry fallback for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_permission_check: false # Skip push permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_pull_permission_check: false # Skip pull permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_with_digest_file: remote-image-name-with-digest.txt # Image name with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_tag_with_digest_file: remote-image-name-tag-with-digest.txt # Image name tag with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_oci_layout_path: remote-oci-layout # OCI layout path for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_single_snapshot: false # Single snapshot for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_tar_path_output: remote-image.tar # Tar path output for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_use_legacy_kaniko_snapshotter: false # Use legacy Kaniko snapshotter for remote cache (if building from source with Kaniko)
          # build_kaniko_cache_remote_cleanup: false # Cleanup after remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_compressed_caching: true # Compressed caching for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_label: '{"key":"value"}' # Image label for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_format: text # Log format for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_timestamp: false # Log timestamp for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_retry_count: 0 # Push retry count for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_mirror: mirror.gcr.io # Registry mirror for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_certificate: /path/to/cert # Registry certificate for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_insecure: false # Insecure remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_skip_tls_verify: false # Skip TLS verify for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_skip_default_registry_fallback: false # Skip default registry fallback for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_permission_check: false # Skip push permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_pull_permission_check: false # Skip pull permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_with_digest_file: remote-image-name-with-digest.txt # Image name with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_tag_with_digest_file: remote-image-name-tag-with-digest.txt # Image name tag with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_oci_layout_path: remote-oci-layout # OCI layout path for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_single_snapshot: false # Single snapshot for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_tar_path_output: remote-image.tar # Tar path output for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_use_legacy_kaniko_snapshotter: false # Use legacy Kaniko snapshotter for remote cache (if building from source with Kaniko)
          # build_kaniko_cache_remote_cleanup: false # Cleanup after remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_compressed_caching: true # Compressed caching for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_label: '{"key":"value"}' # Image label for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_format: text # Log format for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_timestamp: false # Log timestamp for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_retry_count: 0 # Push retry count for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_mirror: mirror.gcr.io # Registry mirror for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_certificate: /path/to/cert # Registry certificate for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_insecure: false # Insecure remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_skip_tls_verify: false # Skip TLS verify for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_skip_default_registry_fallback: false # Skip default registry fallback for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_permission_check: false # Skip push permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_pull_permission_check: false # Skip pull permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_with_digest_file: remote-image-name-with-digest.txt # Image name with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_tag_with_digest_file: remote-image-name-tag-with-digest.txt # Image name tag with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_oci_layout_path: remote-oci-layout # OCI layout path for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_single_snapshot: false # Single snapshot for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_tar_path_output: remote-image.tar # Tar path output for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_use_legacy_kaniko_snapshotter: false # Use legacy Kaniko snapshotter for remote cache (if building from source with Kaniko)
          # build_kaniko_cache_remote_cleanup: false # Cleanup after remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_compressed_caching: true # Compressed caching for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_label: '{"key":"value"}' # Image label for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_format: text # Log format for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_timestamp: false # Log timestamp for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_retry_count: 0 # Push retry count for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_mirror: mirror.gcr.io # Registry mirror for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_certificate: /path/to/cert # Registry certificate for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_insecure: false # Insecure remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_skip_tls_verify: false # Skip TLS verify for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_skip_default_registry_fallback: false # Skip default registry fallback for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_permission_check: false # Skip push permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_pull_permission_check: false # Skip pull permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_with_digest_file: remote-image-name-with-digest.txt # Image name with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_tag_with_digest_file: remote-image-name-tag-with-digest.txt # Image name tag with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_oci_layout_path: remote-oci-layout # OCI layout path for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_single_snapshot: false # Single snapshot for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_tar_path_output: remote-image.tar # Tar path output for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_use_legacy_kaniko_snapshotter: false # Use legacy Kaniko snapshotter for remote cache (if building from source with Kaniko)
          # build_kaniko_cache_remote_cleanup: false # Cleanup after remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_compressed_caching: true # Compressed caching for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_label: '{"key":"value"}' # Image label for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_format: text # Log format for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_timestamp: false # Log timestamp for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_retry_count: 0 # Push retry count for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_mirror: mirror.gcr.io # Registry mirror for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_certificate: /path/to/cert # Registry certificate for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_insecure: false # Insecure remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_skip_tls_verify: false # Skip TLS verify for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_skip_default_registry_fallback: false # Skip default registry fallback for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_permission_check: false # Skip push permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_pull_permission_check: false # Skip pull permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_with_digest_file: remote-image-name-with-digest.txt # Image name with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_tag_with_digest_file: remote-image-name-tag-with-digest.txt # Image name tag with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_oci_layout_path: remote-oci-layout # OCI layout path for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_single_snapshot: false # Single snapshot for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_tar_path_output: remote-image.tar # Tar path output for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_use_legacy_kaniko_snapshotter: false # Use legacy Kaniko snapshotter for remote cache (if building from source with Kaniko)
          # build_kaniko_cache_remote_cleanup: false # Cleanup after remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_compressed_caching: true # Compressed caching for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_label: '{"key":"value"}' # Image label for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_format: text # Log format for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_timestamp: false # Log timestamp for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_retry_count: 0 # Push retry count for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_mirror: mirror.gcr.io # Registry mirror for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_certificate: /path/to/cert # Registry certificate for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_insecure: false # Insecure remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_skip_tls_verify: false # Skip TLS verify for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_skip_default_registry_fallback: false # Skip default registry fallback for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_permission_check: false # Skip push permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_pull_permission_check: false # Skip pull permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_with_digest_file: remote-image-name-with-digest.txt # Image name with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_tag_with_digest_file: remote-image-name-tag-with-digest.txt # Image name tag with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_oci_layout_path: remote-oci-layout # OCI layout path for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_single_snapshot: false # Single snapshot for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_tar_path_output: remote-image.tar # Tar path output for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_use_legacy_kaniko_snapshotter: false # Use legacy Kaniko snapshotter for remote cache (if building from source with Kaniko)
          # build_kaniko_cache_remote_cleanup: false # Cleanup after remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_compressed_caching: true # Compressed caching for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_label: '{"key":"value"}' # Image label for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_format: text # Log format for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_timestamp: false # Log timestamp for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_retry_count: 0 # Push retry count for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_mirror: mirror.gcr.io # Registry mirror for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_certificate: /path/to/cert # Registry certificate for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_insecure: false # Insecure remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_skip_tls_verify: false # Skip TLS verify for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_skip_default_registry_fallback: false # Skip default registry fallback for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_permission_check: false # Skip push permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_pull_permission_check: false # Skip pull permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_with_digest_file: remote-image-name-with-digest.txt # Image name with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_tag_with_digest_file: remote-image-name-tag-with-digest.txt # Image name tag with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_oci_layout_path: remote-oci-layout # OCI layout path for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_single_snapshot: false # Single snapshot for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_tar_path_output: remote-image.tar # Tar path output for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_use_legacy_kaniko_snapshotter: false # Use legacy Kaniko snapshotter for remote cache (if building from source with Kaniko)
          # build_kaniko_cache_remote_cleanup: false # Cleanup after remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_compressed_caching: true # Compressed caching for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_label: '{"key":"value"}' # Image label for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_format: text # Log format for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_timestamp: false # Log timestamp for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_retry_count: 0 # Push retry count for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_mirror: mirror.gcr.io # Registry mirror for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_certificate: /path/to/cert # Registry certificate for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_insecure: false # Insecure remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_skip_tls_verify: false # Skip TLS verify for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_skip_default_registry_fallback: false # Skip default registry fallback for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_permission_check: false # Skip push permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_pull_permission_check: false # Skip pull permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_with_digest_file: remote-image-name-with-digest.txt # Image name with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_tag_with_digest_file: remote-image-name-tag-with-digest.txt # Image name tag with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_oci_layout_path: remote-oci-layout # OCI layout path for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_single_snapshot: false # Single snapshot for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_tar_path_output: remote-image.tar # Tar path output for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_use_legacy_kaniko_snapshotter: false # Use legacy Kaniko snapshotter for remote cache (if building from source with Kaniko)
          # build_kaniko_cache_remote_cleanup: false # Cleanup after remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_compressed_caching: true # Compressed caching for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_label: '{"key":"value"}' # Image label for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_format: text # Log format for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_timestamp: false # Log timestamp for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_retry_count: 0 # Push retry count for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_mirror: mirror.gcr.io # Registry mirror for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_certificate: /path/to/cert # Registry certificate for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_insecure: false # Insecure remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_skip_tls_verify: false # Skip TLS verify for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_skip_default_registry_fallback: false # Skip default registry fallback for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_permission_check: false # Skip push permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_pull_permission_check: false # Skip pull permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_with_digest_file: remote-image-name-with-digest.txt # Image name with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_tag_with_digest_file: remote-image-name-tag-with-digest.txt # Image name tag with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_oci_layout_path: remote-oci-layout # OCI layout path for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_single_snapshot: false # Single snapshot for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_tar_path_output: remote-image.tar # Tar path output for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_use_legacy_kaniko_snapshotter: false # Use legacy Kaniko snapshotter for remote cache (if building from source with Kaniko)
          # build_kaniko_cache_remote_cleanup: false # Cleanup after remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_compressed_caching: true # Compressed caching for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_label: '{"key":"value"}' # Image label for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_format: text # Log format for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_timestamp: false # Log timestamp for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_retry_count: 0 # Push retry count for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_mirror: mirror.gcr.io # Registry mirror for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_certificate: /path/to/cert # Registry certificate for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_insecure: false # Insecure remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_skip_tls_verify: false # Skip TLS verify for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_skip_default_registry_fallback: false # Skip default registry fallback for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_permission_check: false # Skip push permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_pull_permission_check: false # Skip pull permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_with_digest_file: remote-image-name-with-digest.txt # Image name with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_tag_with_digest_file: remote-image-name-tag-with-digest.txt # Image name tag with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_oci_layout_path: remote-oci-layout # OCI layout path for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_single_snapshot: false # Single snapshot for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_tar_path_output: remote-image.tar # Tar path output for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_use_legacy_kaniko_snapshotter: false # Use legacy Kaniko snapshotter for remote cache (if building from source with Kaniko)
          # build_kaniko_cache_remote_cleanup: false # Cleanup after remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_compressed_caching: true # Compressed caching for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_label: '{"key":"value"}' # Image label for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_format: text # Log format for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_timestamp: false # Log timestamp for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_retry_count: 0 # Push retry count for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_mirror: mirror.gcr.io # Registry mirror for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_certificate: /path/to/cert # Registry certificate for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_insecure: false # Insecure remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_skip_tls_verify: false # Skip TLS verify for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_skip_default_registry_fallback: false # Skip default registry fallback for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_permission_check: false # Skip push permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_pull_permission_check: false # Skip pull permission check for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_with_digest_file: remote-image-name-with-digest.txt # Image name with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_name_tag_with_digest_file: remote-image-name-tag-with-digest.txt # Image name tag with digest file for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_oci_layout_path: remote-oci-layout # OCI layout path for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_single_snapshot: false # Single snapshot for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_tar_path_output: remote-image.tar # Tar path output for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_use_legacy_kaniko_snapshotter: false # Use legacy Kaniko snapshotter for remote cache (if building from source with Kaniko)
          # build_kaniko_cache_remote_cleanup: false # Cleanup after remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_compressed_caching: true # Compressed caching for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_image_label: '{"key":"value"}' # Image label for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_format: text # Log format for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_log_timestamp: false # Log timestamp for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_push_retry_count: 0 # Push retry count for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_mirror: mirror.gcr.io # Registry mirror for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_certificate: /path/to/cert # Registry certificate for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_insecure: false # Insecure remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_registry_skip_tls_verify: false # Skip TLS verify for remote cache for Kaniko (if building from source with Kaniko)
          # build_kaniko_cache_remote_skip_default_registry
